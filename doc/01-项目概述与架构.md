# 项目概述与架构

## 技术栈

- **语言 / 平台**：Java 21
- **框架**：Spring Boot 4.1.1
- **持久层**：Spring Data JPA + MySQL（`ddl-auto=update`）
- **缓存**：Spring Data Redis（Lettuce）
- **网络（代理）**：Netty（NioIoHandler / MultiThreadIoEventLoopGroup）
- **其他**：Lombok、fastjson2、OkHttp、Quartz、Spring Statemachine、spring-boot-starter-mail / websocket / actuator

## 顶层模块

```
src/main/java/moe/koseirin/nyanruaineo/
├── NyanIdApplication.java      # 启动入口（@EnableScheduling / @EnableAsync / @EnableJpaRepositories）
├── entity/                     # JPA 实体
├── repository/                 # Spring Data JPA 仓储
├── dto/                        # 请求/响应 DTO
├── server/                     # REST 控制器（V1~V5、AI、YggdrasilServer）
├── services/                   # 业务服务接口与实现（services.impl）
├── utils/                      # 工具（Redis、SqlService、System、WebMvc、ErrorUtils…）
├── Minecraft/                  # Minecraft 代理（Netty）
│   ├── MinecraftProxy.java     #   代理启动/停止、在线玩家集合
│   ├── command/                #   代理命令框架（/server、/kick、/ban…）
│   ├── connection/             #   UserConnection / ServerConnection
│   ├── handler/                #   InitialHandler / UpstreamBridge / DownstreamBridge / ServerConnector
│   ├── netty/                  #   Varint21FrameDecoder / PacketDecoder / FirewallHandler…
│   ├── protocol/               #   协议、数据包（Packet）、版本常量
│   ├── service/                #   代理服务（Firewall / TabList / Transfer / Ban / PluginMessage…）
│   ├── config/                 #   代理配置（ProxyProperties + cfg/*）
│   ├── forge/                  #   Forge (FML) 握手支持
│   └── event/ + listener/      #   事件总线与事件
└── eventbus/                   # 事件总线
```

## 数据模型（核心实体）

| 实体 | 说明 |
| --- | --- |
| `Accounts` | 主账号（uid、email、username、password、bind、MicrosoftAccount…） |
| `NyanIDuser` | 账号扩展资料（exp、nickname、头像、Description…） |
| `UserDevices` | 登录设备（token、session、ClientId、uid…） |
| `Yggdrasil` | 外置登录角色（uuid ↔ nyanuid、playername、useSkin/useCAPE） |
| `YggdrasilPlayer` | 角色皮肤/披风纹理哈希 |
| `BanUserList` | 封禁记录（uid/uuid、Type、TargetType、ExpireTime…） |
| `Permission` | 权限节点目录（id、code、name） |
| `UserPermissions` | 用户权限授权（uid + permission 复合主键） |
| `SystemConfig` | 动态配置键值（`proxy.*` 等） |

## 关键概念

### UID 与 Minecraft UUID

- **NyanID UID**：32 位十六进制字符串（`utilset.GenerateUUID`，去除连字符），是 `Accounts.uid`。
- **Minecraft UUID**：Yggdrasil / Mojang 的玩家 UUID（`Yggdrasil.uuid` 存储带连字符的 36 位）。
- 二者通过 `Yggdrasil.nyanuid` / `Yggdrasil.uuid` 建立映射；`Accounts.bind` 也可作为绑定来源。

### 认证链路

1. 用户登录拿到 RSA 加密的 `accessToken`（对应 `UserDevices.Token`）。
2. Web API 通过 `Authorization: Bearer <加密token>` 头传递，服务端用 `utilset.decrypt(..., privateKey)` 解密出原始 token，再查 `UserDevicesRepository.findUidByToken` 得到 uid。
3. 某些端点还会经过 `AuthenticateCheck` 拦截器校验 `Event` 头与 HTTP 方法是否匹配。

### 权限节点

见 [08-权限与封禁系统](./08-权限与封禁系统.md)。授权按节点字符串匹配，支持 `*` 根节点与 `prefix.*` 通配。

### 封禁

见 [08-权限与封禁系统](./08-权限与封禁系统.md)。游戏封禁区分「Yggdrasil 玩家 → 封 NyanID UID」「正版玩家 → 封 UUID」，支持过期自动解封。
